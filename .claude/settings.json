{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "b=$(git symbolic-ref --quiet --short HEAD 2>/dev/null) || exit 0; case \"$b\" in main|master) exit 0 ;; esac; git push -u origin \"$b\"",
            "statusMessage": "Pushing branch to origin…",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
